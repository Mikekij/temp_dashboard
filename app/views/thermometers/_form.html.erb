<%= form_with(model: thermometer, local: true) do |form| %>
  <% if thermometer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(thermometer.errors.count, "error") %> prohibited this thermometer from being saved:</h2>

      <ul>
      <% thermometer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :thermometer_name %>
  </div>

  <div class="field">
    <%= form.label :serial %>
    <%= form.text_field :serial, id: :thermometer_serial %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :thermometer_location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
